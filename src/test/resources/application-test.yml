spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_goldxdb
    username: test_user
    password: test1234
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379