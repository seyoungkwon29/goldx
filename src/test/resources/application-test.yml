spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: "postgres"
    password: "0000"
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379